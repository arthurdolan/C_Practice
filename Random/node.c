#include <stdio.h>

#define MAXAGE = 20;
struct location{
    char name[MAXAGE];
    int longditude;
    int latitude;
}
void add_new_location(struct location newLoc, struct location *list, int *numLocations );
    if(numblocations<10){
        strcpy(list[*numLocations].name, newloc.name);
        list[*numLocations].longditude, newLoc.longditude;
        list[*numLocations].latitude, newLoc.latitude;
        printf("%s",list[*numlocations].name);
        *numlocations +=1 
        return 1;
int print_location()

        
    }
    else {
        return 0;
    }

int main(){
    struct locations arrayOfLocations[10];
    int numLocations = 0;
    struct location arrayOfLocations[0]
    struct location location 1;
    str cop

}

